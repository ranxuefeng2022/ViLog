/**
 * 右键菜单模块
 * 负责上下文菜单
 */

window.App = window.App || {};
window.App.ContextMenu = {
  init: function() {
    console.log('Context Menu initialized');
    // 右键菜单功能将在后续版本中实现
  }
};

console.log('✓ Context Menu module loaded');
